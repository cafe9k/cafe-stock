# ⚡ 5分钟快速开始

## 1️⃣ 安装依赖 (已完成 ✓)

```bash
npm install
```

## 2️⃣ 配置 Supabase

### 获取凭证

1. 访问 → https://supabase.com
2. 创建项目 → 等待 2 分钟
3. 进入 **Settings** → **API**
4. 复制:
   - Project URL
   - anon/public key

### 配置本地

编辑 `.env.local`:

```env
VITE_SUPABASE_URL=粘贴你的URL
VITE_SUPABASE_ANON_KEY=粘贴你的Key
```

## 3️⃣ 初始化数据库

1. Supabase 仪表板 → **SQL Editor**
2. 复制 `database/init.sql` 全部内容
3. 粘贴并点击 **Run**
4. 看到 "Success" ✓

## 4️⃣ 启动应用

```bash
npm run dev
```

🎉 打开浏览器: http://localhost:3000

## 验证清单

- [ ] 看到 "✓ 数据库已连接" (绿色)
- [ ] 能看到示例数据
- [ ] 能添加新库存项
- [ ] 能删除库存项

## 出问题了?

### 连接失败 ❌

1. 检查 `.env.local` 配置
2. 确认 Supabase 项目正常运行
3. 重新运行 `database/init.sql`

### 查看详细文档

- 📖 [完整使用指南](USAGE.md)
- 🔧 [详细安装步骤](SETUP.md)
- 📚 [项目文档](README.md)

---

**核心命令**

```bash
npm run dev      # 启动开发服务器
npm run build    # 构建生产版本
npm run preview  # 预览生产构建
```

**Supabase URL**
https://supabase.com/dashboard

**本地地址**
http://localhost:3000

**项目结构**

```
cafe-stock/
├── src/
│   ├── App.tsx              # 主应用
│   ├── lib/supabaseClient.ts # 数据库客户端
│   └── config/supabase.ts   # 配置
├── database/
│   └── init.sql             # 数据库初始化
├── .env.local               # 环境变量 ← 配置这里!
└── package.json
```

**常用操作**

| 操作 | 说明 |
|------|------|
| 添加库存 | 填表单 → 点击"添加库存" |
| 刷新数据 | 点击"刷新数据"按钮 |
| 删除项目 | 点击"删除"按钮 → 确认 |

**数据库表结构**

```sql
stock_items
├── id (自增主键)
├── name (名称)
├── quantity (数量)
├── unit (单位)
├── category (类别)
├── created_at (创建时间)
└── updated_at (更新时间)
```

现在开始使用吧! 🚀
